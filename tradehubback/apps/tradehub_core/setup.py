from setuptools import setup, find_packages

setup(
    name='tradehub_core',
    version='0.0.1',
    description='TradeHub Core Platform',
    author='TradeHub',
    author_email='admin@tradehub.com',
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
